#include "SuccinctRank.h"
#include "Utility.h"       // You'll likely want what's in here.
#include <cmath>           // For log2, which we assume you'll need.
#include <iostream>
using namespace std;

SuccinctRank::SuccinctRank(const void* bits, uint64_t numBits) {
    /* TODO: Delete this comment and the next two lines, then implement
     * this function.
     */
    (void) bits;
    (void) numBits;
}

uint64_t SuccinctRank::rank(std::uint64_t bitIndex) const {
    /* TODO: Delete this comment and the next two lines, then implement
     * this function.
     */
    (void) bitIndex;
    return -1;
}
